# Optional: GitHub personal access token (no scopes needed — public repo reads only).
# Raises the GitHub API rate limit from 60/hr per IP to 5000/hr.
# Set this in Vercel's environment variables for production.
GITHUB_TOKEN=
